type Query {
  # Listado de categorias
  ListadoCategorias(
    "Página a la que accederemos"
    page: Int = 1, 
    "Numero de elementos por página que queremos"
    itemsPage: Int = 20): ResultCategorias

  # Muestra la información de una categoria en concreto
  DetalleCategoria(idCategoria: Int!): ResultCategoria
}